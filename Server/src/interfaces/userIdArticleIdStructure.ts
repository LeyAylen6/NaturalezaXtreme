export interface userIdArticleId {
    userId: number, 
    articleId: number,
    size: string,
    quantity: number
}

// SHOPPING CART

    // id: number
    
    // @Column({
    //     type: "enum",
    //     enum: ["pending", "complete" ],
    //     default: "complete"
    // })
    // status: CategoryCart

    // @Column()
    // userId: number